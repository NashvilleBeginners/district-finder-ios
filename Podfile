# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'district_finder' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for district_finder
  pod 'Argo'
  pod 'Curry'
  pod 'Runes'

  target 'district_finderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'district_finderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
